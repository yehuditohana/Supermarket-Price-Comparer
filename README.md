# Supermarket-Price-Comparer
Full-stack supermarket price comparison tool. Backend: Java application using Spring Boot as a lightweight container. APIs are exposed via JAX-RS. Data persistence with JPA and parsing using SAX. Frontend: Dynamic UI developed with JSX (React).
